#include <stdio.h>

int addNumbers(int n1, int n2) {
  int sum;
  sum = n1 + n2;
  return sum; // return an int value
}

int main() {
  int a = 6, b = 8, result;

  // return value is stored in result
  result = addNumbers(a, b);
  printf("sum = %d", result); // 14

  return 0;
}
